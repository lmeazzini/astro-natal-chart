/**
 * Placeholder test file to ensure vitest can collect tests.
 * TODO: Remove this file when actual tests are implemented.
 */

import { describe, it, expect } from 'vitest';

describe('Placeholder', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});
